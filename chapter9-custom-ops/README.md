CUDA custom operations
===
## How to install
```shell
pip install -r requirement.txt
pip install -e .
```

## How to test
Inside each operation in `cuda_ops/` directory, there is a `test.py`.
```shell
python test.py
```
